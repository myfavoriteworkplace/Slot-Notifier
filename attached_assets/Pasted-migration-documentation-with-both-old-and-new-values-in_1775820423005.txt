migration documentation with both old and new values included, so you have a clear record of what was changed:
🌐 DNS Changes
Added CNAME records at registrar:
bookmyslot.dental.mossaic.in → book-my-slot-client.onrender.com (frontend)
api.bookmyslot.dental.mossaic.in → book-my-slot-1.onrender.com (backend)
Initially added both www.bookmyslot.dental.mossaic.in and non‑www, then decided to keep non‑www as primary and redirect www → non‑www.
TTL set to 300–600 seconds during migration, later increased to 3600 seconds for stability.
⚙️ Render Configuration
Frontend service
Custom domain: bookmyslot.dental.mossaic.in (primary), optional www.bookmyslot.dental.mossaic.in.
SSL certificates provisioned automatically.
Environment variable:
Old: VITE_API_URL = https://book-my-slot-1.onrender.com
New: VITE_API_URL = https://api.bookmyslot.dental.mossaic.in  
(ensured no trailing slash)
Backend service
Custom domain: api.bookmyslot.dental.mossaic.in.
SSL verified quickly.
Environment variable:
Old: FRONTEND_URL = https://book-my-slot-client.onrender.com
New: FRONTEND_URL = https://bookmyslot.dental.mossaic.in  
(supports comma‑separated values for multiple origins)
🛠 Code Changes
server/index.ts
Updated CORS configuration to explicitly allow:
https://bookmyslot.dental.mossaic.in
https://www.bookmyslot.dental.mossaic.in
https://api.bookmyslot.dental.mossaic.in
Original Render URLs (kept for safety).
Modified logic so FRONTEND_URL env var supports comma‑separated values.
routes.ts
Confirmed FRONTEND_URL is used for generating consent/email links.
Ensured patient email links now point to the correct custom domain.
✅ Verification & Testing
Used curl to test backend health endpoint:
curl https://api.bookmyslot.dental.mossaic.in/api/health
Initially saw 500 errors → fixed by updating CORS and environment variables.
Confirmed frontend → backend communication works after redeployment.
Verified SSL certificates active for both frontend and backend.
📌 Final Setup
Frontend (patient‑facing UI) → https://bookmyslot.dental.mossaic.in
Backend (API endpoints) → https://api.bookmyslot.dental.mossaic.in
CORS & Email links → Configured via FRONTEND_URL env var.
DNS → Clean, stable CNAME records with proper TTL.
SSL → Auto‑provisioned by Render, forced HTTPS enabled.
👉 This document now captures the before and after values, the DNS changes, the Render environment updates, and the code modifications.