## Packages
date-fns | Date manipulation for calendar and slots
lucide-react | Icons for the interface (already in stack but good to confirm)
clsx | Class name utility
tailwind-merge | Class name utility

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-sans)"],
}
